#pragma once
#define ERROR 0
int menu();

bool matriplus();    
bool nummulti();
bool matritrans();
bool matrimulti();
bool hadamulti();
bool conv();
bool conv_eg(int*& image_matrix, int choice);

bool demo();
bool OTSU();
bool plus_code(const char* str, int closed_size = 5, int open_size = 3);
bool free_act();
